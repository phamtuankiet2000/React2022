function AddComponents(props) {
    return (
        <div className="container">
            <h1>Total: {props.firtsNumber + props.secondNumber}</h1>
        </div>
    )
}
export default AddComponents;